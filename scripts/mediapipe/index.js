export function initMediaPipeline({ editor }) {
  console.info("[mediamime] MediaPipe module initialised (stub).");
  return {
    dispose() {
      console.info("[mediamime] MediaPipe module disposed.");
    }
  };
}
